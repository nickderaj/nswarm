[profile.default]
fail-fast = true
retries = 0
slow-timeout = { period = "30s", terminate-after = 2 }

[profile.ci]
fail-fast = false
retries = 0
status-level = "pass"
final-status-level = "flaky"
junit = { path = "junit.xml" }
